# Data-Home
# unzip the file, then:
cd datavault-vercel
git init
git add index.html
git commit -m "feat: Carbon Clarity Data Vault MVP"
git remote add origin https://github.com/YOUR_USERNAME/data-vault.git
git push -u origin main
